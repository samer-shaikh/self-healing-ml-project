from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='i am building the self healing model with multiple models',
    author='samer',
    license='',
)
